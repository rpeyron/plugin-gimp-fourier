# Missing headers to download from GIMP repo

libgimpbase/gimpchoice.h (https://gitlab.gnome.org/GNOME/gimp/-/issues/11454,https://gitlab.gnome.org/GNOME/gimp/-/issues/10900)

libgimp/stdplugins-intl.h
libgimpwidgets/gimplabelstringwidget.h

# Test with hot.c

replace config.h with
#define GETTEXT_PACKAGE "glib"

# Glib-2.0

Major change in glib2 2.79.1  !3826 genums: use g_once_init_enter_pointer for GType initializers (https://gitlab.gnome.org/GNOME/glib/blob/2.79.1/NEWS)
gimp 2.99.18 is shipped with 2.78 
mingw64 has now the version 2.80
so binary are incompatible, copy libglib-2.0-0.dll in same folder as the plugin fix the problem

# News

plugin must be in a folder
plugin exe must have the same name as the folder
